export const dynamic = "force-dynamic";

export default function CreateRecordPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1 style={{ fontSize: 24, fontWeight: 700 }}>Create · Record</h1>
      <p style={{ marginTop: 8, opacity: 0.85 }}>
        Record flow shell (CI-safe). Recording engine attaches after permissions + device harness.
      </p>
    </main>
  );
}
