export const dynamic = "force-dynamic";

export default function CreatePortalPage() {
  return (
    <main style={{ padding: 24 }}>
      <h1 style={{ fontSize: 24, fontWeight: 700 }}>Create</h1>
      <p style={{ marginTop: 8, opacity: 0.85 }}>
        Create portal is online (CI-safe shell). Advanced modules load behind flags.
      </p>
    </main>
  );
}
