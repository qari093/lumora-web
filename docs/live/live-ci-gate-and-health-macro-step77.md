# Live — CI Gate + Health Macro (Step 77)

## CI Gate (no server needed)
```sh
sh scripts/live/ci_gate.sh
```

## Local Health Macro (server must be running)
```sh
PORT=3000 sh scripts/live/health_macro.sh
```
