# Session Personalization Engine

- Generates deterministic session seed
- Shuffles feed per session
- Prevents repetition across sessions
- Attaches session context to items
- Creates unique user experience per session
