import { ensureServerReady } from "../helpers/ensureServer";

const base = process.env.BASE_URL || process.env.NEXT_PUBLIC_BASE_URL || "http://127.0.0.1:3000";
process.env.BASE_URL = base;

await ensureServerReady({ baseUrl: process.env.BASE_URL, maxWaitMs: 20000, pollMs: 250, port: 3000 });
