# Polaroid MVP — Mobile Smoke Workflow Tool (Step 95)

**UTC:** 2025-12-20T20:22:33Z

## Purpose
Provides a single, repeatable command to:
1) Confirm local origin health
2) Issue a fresh quick tunnel URL
3) Print iPhone instructions + exact record/lock commands

## Command
```bash
polaroid-mvp/tools/polaroid-mobile-smoke.sh
```

## Expected Output
- Prints `LIVE_URL=` for iPhone Safari
- Prints the exact `record-mobile-result.sh` and `lock-mobile-result.sh` commands for step **95**
