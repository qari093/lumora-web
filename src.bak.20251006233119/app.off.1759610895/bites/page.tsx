export default function Page(){
  return (
    <div style={{padding:24,fontFamily:"-apple-system,Inter,Segoe UI,Roboto,Arial"}}>
      <h1 style={{margin:0}}>Bites — AI Video Studio (MVP)</h1>
      <p style={{opacity:.7}}>Route is alive. API is below at <code>/api/bites/plan</code>.</p>
    </div>
  );
}
