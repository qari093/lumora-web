export function getSimulatedCirclePreview() {
  return {
    participants: 5,
    mood: "calm",
    durationMinutes: 12,
    description: "A quiet space where your content is witnessed without noise.",
  };
}
