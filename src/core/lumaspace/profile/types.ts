export interface HeroSpark {
  id: string;
  aura: string;
}

export interface TimelineWave {
  id: string;
  depth: number;
}

export interface ProfileRuntime {
  active: boolean;
  profileId: string;
}
