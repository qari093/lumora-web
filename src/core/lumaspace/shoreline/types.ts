export interface ShorelineSpark {
  id: string;
  emotion: string;
}

export interface GravityWell {
  id: string;
  pull: number;
}

export interface ShorelineRuntime {
  active: boolean;
  feedId: string;
}
