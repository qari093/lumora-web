export function createVideoChecksum(input: string): string {
  let hash = 0;

  for (let i = 0; i < input.length; i += 1) {
    hash = (hash << 5) - hash + input.charCodeAt(i);
    hash |= 0;
  }

  return `sha256_sim_${Math.abs(hash).toString(36)}`;
}
